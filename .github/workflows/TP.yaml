name: CI
on: 
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
      
    - name: Compiler le programme
      run: |
        bash CMakeGCC.sh
        cd bin/make
        make
        cd Release
        ./sobel